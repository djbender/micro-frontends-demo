{
  "name": "mfe-demo",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "26.2.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "preview": "pnpm -r --parallel run preview"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
